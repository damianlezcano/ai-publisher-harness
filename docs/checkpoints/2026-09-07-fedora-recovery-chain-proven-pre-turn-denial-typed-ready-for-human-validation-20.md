## FEDORA RECOVERY CHAIN PROVEN — PRE-TURN DENIAL TYPED, READY FOR HUMAN VALIDATION (2026-09-07)

- **Starting tree:** `21c5b4b` (clean). Objective: prove the instrumented recovery chain in a real running EducAI instance and name the first missing boundary before packaging.
- **Root cause (proven in the real runtime):** the operator's 52-file accepted Send was killed **between material copy and user-turn persistence**. The durable ledger held `turn_id=NULL`, `state=copying`, `agent_state=not_started`, `prepared=52 / indexed=0 / ready=0`, and **no user message**. `agent_resume_import`'s synchronous turn-id pre-validation then failed with a generic `internal` error before `resume_requested` was ever logged — the first missing event. In this state the prompt was never durably recorded, so local resume is genuinely impossible (nothing to reuse).
- **Fix (bounded, no ledger redesign):** the pre-turn case is now a first-class typed denial. `resume_accepted_import_operation` logs `operation_loaded` before the turn-id check and returns `ErrorCode::RecoveryNoTurn` (`recovery_no_turn`) with `resume_decision=denied reason=no_turn`, instead of a generic internal error. The command threads the (possibly empty) turn id through and lets the resume thread deliver the typed denial via a new `code` field on `agent://task`. The frontend shows truthful copy **"El envío se interrumpió antes de confirmarse; volvé a enviarlo."** (no `Reintentar`, which would be futile) for `recovery_no_turn`; retryable failures keep `resumeFailed` + `Reintentar`.
- **Observability (§6):** a narrow `session_log_record` command (accepts only `[recovery-ui]`-prefixed structural lines) bridges the frontend recovery-ui trace into the same `--debug` stderr/session log, so one log shows the whole chain end to end.
- **Real-runtime proof — terminal case (operator state, `turn_id=NULL`):** observed in order `[recovery-ui] detected → invoking → command_received → resume_requested → operation_loaded (copying/52/0/0/not_started) → resume_decision=denied reason=no_turn → [knowledge][ERROR] failure_class=no_turn`; the frontend rendered the no-turn copy (confirmed by OCR of the live window).
- **Real-runtime proof — allowed case (`turn_id` set, controlled seam):** a production-faithful harness (`AppState::send_staged_message_persist` with 3 synthetic files, then stop before indexing) produced `turn_id` set / `state=copying`; reopening the real app observed `… → resume_decision=allowed reason=safe_local_not_started → local_resume_started → lexical_progress completed=3 → embedding_progress`. Durable result: `lexical_completed=3`, exactly one user turn, no duplicate materials; the (model-less) remote step then left `state=pending_retry / agent_state=started_outcome_unknown` (never auto-resend). **§12 gate PASS.**
- **Safety contract preserved:** `StartedOutcomeUnknown` is still never auto-resent; the no-turn denial is terminal and never fabricates a turn; completed remote state is not resent; duplicate turn/material prevention re-verified (1 turn, N materials after resume).
- **Validation:** `cargo fmt --check` PASS; `cargo clippy -D warnings` (knowledge/app/agent + tauri crate) PASS; `cargo test -p project-knowledge/-project-app/-project-agent` and `cargo test --workspace` green; frontend 278 tests + typecheck/lint/format/build green; `git diff --check` PASS. `./scripts/verify` stops only on the two documented pre-existing Windows-only `unused_mut` findings in unchanged `project-tunnel`/`project-opencode` lib-test code.
- **Gate status:** recovery chain PROVEN in a real running instance for both the terminal denial and the allowed local resume (`indexed>0`). **READY FOR FEDORA HUMAN VALIDATION.** Windows Knowledge remains NOT YET HUMAN-VALIDATED; M11 NOT STARTED; NOT PUSHED.
