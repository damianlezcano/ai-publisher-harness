## WINDOWS CLOUDFLARED ENVIRONMENT FIX PASS — SYSTEMROOT forwarded to the cloudflared child on Windows only; regression + native NSIS rebuild + silent install + human Share retest with real Quick Tunnel + Stop-sharing proof = ALL PASS — READY TO CONTINUE WINDOWS HUMAN RUNTIME VALIDATION (2026-09-05)

- **Scope:** bounded Windows-only runtime fix + regression + rebuild + install + real Share/Stop-share proof. **Proven root cause (do NOT reopen):** EducAI's process supervisor `env_clear()`s and reconstructs a minimal child environment (PATH/HOME only) that omitted `SYSTEMROOT` on Windows. A controlled differential with the installed pinned cloudflared 2026.8.3 proved PATH+HOME only → DNS/getaddrinfo failure → `Post "https://api.trycloudflare.com/tunnel": dial tcp: lookup api.trycloudflare.com: getaddrinfo` → Quick Tunnel FAIL / exit code 1; PATH+HOME+SYSTEMROOT → DNS succeeds → Quick Tunnel URL produced → cloudflared stays alive. Exact fix: on Windows only, forward the parent `SYSTEMROOT` value verbatim into the reconstructed cloudflared child env after clearing. No tunnel redesign, no cloudflared version change, no publication filesystem change, no OpenCode change, no M11, no Knowledge.
- **Author implementation commit:** `f67a010` `fix(windows): preserve SYSTEMROOT for cloudflared child` — `crates/project-tunnel/src/cloudflare.rs` only. Inline env construction in `CloudflareQuickTunnel::start_inner` is factored into a new testable `build_child_env(extra_env)` that returns exactly PATH + HOME + (Windows-only) `windows_systemroot_env()` + test-only `extra_env`. `#[cfg(windows)]` forwards `std::env::var("SYSTEMROOT")` verbatim (parent value, never a hardcoded `C:\Windows`); `#[cfg(not(windows))]` forwards nothing (Linux unchanged). This mirrors the proven OpenCode child policy (`6337c02`). Env isolation preserved: `ChildGuard::spawn` still `.env_clear()`s and applies exactly the managed keys; no broad inheritance of USERPROFILE/APPDATA/LOCALAPPDATA/TEMP/TMP/credentials. `CloudflareQuickTunnel` command line and cloudflared version unchanged.
- **Regression tests (in-file unit tests on `build_child_env`, no network):** `child_env_keeps_managed_keys_and_forwards_windows_systemroot` (exact managed key set; SYSTEMROOT forwarded verbatim from parent on Windows; absent on non-Windows), `child_env_preserves_path_and_home_verbatim`, `child_env_appends_extra_env_after_managed_keys` — 3/3 PASS on native Windows.
- **Focused checks (native Windows 11 x64/MSVC, cargo 1.97.1, pnpm 11.25.0, tauri-cli 2.11.4):** `cargo test -p project-tunnel --lib` 3/3 PASS; `cargo test -p project-opencode` 5/5 PASS; `cargo test -p project-tunnel --test cloudflare` 12 pass / 2 pre-existing Windows-host fixture failures (`process_exists` hardcodes Linux `/proc/{pid}`, and `PathBinaryResolver::new("true")` needs the Unix `true` binary) — proven identical at the parent commit `9458e9d` via `git stash`; `cargo test -p project-process` 3 pre-existing `/proc`-based failures, untouched by this pass; `cargo fmt --all -- --check` PASS; `cargo clippy -p project-tunnel --all-targets` PASS (pre-existing dead-code warning in `tunnel_security.rs` only); `git diff --check` PASS. `./scripts/verify` remains not runnable on this Windows host (bash/Linux/WebKit gate), per policy.
- **Build:** `powershell -ExecutionPolicy Bypass -File .\packaging\windows\build.ps1` exit 0. Artifact `app/src-tauri/target/release/bundle/nsis/EducAI_0.1.0_x64-setup.exe`, **59,269,974 bytes**, timestamp **2026-09-05T09:53:18-03:00**, SHA-256 **`72102F0914E6C1C6EBF2ED905340BE947C310AB4C58742728C09DD9EE7322801`**. Previous generated artifact renamed to `EducAI_0.1.0_x64-setup.previous.exe` before rebuild. cloudflared pinned at 2026.8.3 (manifest unchanged). Build-only `packageManager` write-back in `app/package.json` reverted (working tree clean).
- **Install / runtime:** silent NSIS update (`/S /CURRENTUSER`, exit 0) installed the new `educai.exe` into `C:\Users\lezca\AppData\Local\EducAI`; `%APPDATA%\com.educai.publisher` preserved (projects `01a06f16-6abe-7102-9bba-8f72e986f0a2` and `01a0718b-…`, persisted conversation, Creation output, publish tree intact). EducAI launched (PID 14144); owned `opencode.exe` child (PID 4232) reached `GET /global/health` → HTTP 200 `{"healthy":true,"version":"1.18.25"}` on `127.0.0.1:51787`.
- **Human Share retest (operator performed exactly one `Compartir` click):** the app's owned cloudflared child (PID 15908, ParentProcessId = educai 14144) spawned with `tunnel --url http://127.0.0.1:55732/ --no-autoupdate --loglevel info`, **remained alive (no exit code 1)**, established an outbound TLS connection to Cloudflare edge (`104.16.231.132:443`), bound the Quick Tunnel edge socket (`0.0.0.0:55733`), and proxied the local origin (`127.0.0.1:55732`). The operator confirmed the app displayed a public `https://*.trycloudflare.com` URL and it loaded the expected Creation. **Previous DNS failure FIXED:** no `lookup api.trycloudflare.com: getaddrinfo` — cloudflared's live TLS connection to Cloudflare edge is impossible without successful DNS/getaddrinfo. Host `Resolve-DnsName api.trycloudflare.com` returns 4 records.
- **Share pipeline:** 1) Creation source resolution PASS 2) snapshot staging PASS 3) snapshot durability PASS 4) snapshot install/rename PASS 5) local_publish PASS (`http://127.0.0.1:55732/conversacion-nueva-szakzc/` → HTTP 200, 8864 bytes) 6) local HTTP PASS 7) cloudflared spawn PASS (PID 15908, parented to EducAI, alive) 8) Quick Tunnel URL discovery PASS (operator-confirmed public trycloudflare URL) 9) public HTTPS readiness PASS (operator confirmed it loads the Creation).
- **Stop-sharing proof (operator performed exactly one `Dejar de compartir` click):** cloudflared (PID 15908) exited (no cloudflared process remains), local publisher port 55732 closed, EducAI (14144) remained running with only its webview2 + opencode children; unrelated processes untouched (the separate `@opencode-aidesktop\OpenCode.exe` desktop app tree was explicitly left alone).
- **User-visible error:** the misleading "comprobá tu conexión a Internet" copy for the proven child-environment failure is **recorded as DEFERRED** (not fixed in this pass).
- **OBSERVABILITY PARTIAL — DEFERRED:** raw cloudflared stderr is not persisted in the app session log; tunnel lifecycle evidence was captured via process/port/network observation. Not redesigned in this pass.
- **M11 NOT STARTED. Knowledge NOT STARTED.** Overall Windows HUMAN-PASS not claimed; the next step is continuing Windows human runtime validation.
- **Status: WINDOWS CLOUDFLARED ENVIRONMENT FIX PASS — READY TO CONTINUE HUMAN RUNTIME VALIDATION.**
