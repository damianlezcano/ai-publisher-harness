## K6 OPENCODE 1.18.25 COMPLETION POLL — STRUCTURAL COMPLETE, READY FOR FEDORA 5-FILE EXHAUSTIVE-SUMMARY HUMAN VALIDATION (2026-09-08)

- **Starting tree:** `6fa26b6` clean except untracked local `opencode.json` (not committed). Author: Cursor Agent. Independent read-only reviewer: **REVIEW=APPROVE**.
- **Human Fedora evidence (authoritative, post-envelope AppImage `3263c163…`):** five selected Markdown files indexed (`embeddings_created=4655`), date order correct, then ~600s stall. Terminal: `documents=5 summary_nodes_generated=0 remote_summary_calls=0 duration_ms≈600182`. Per-file UI: “No se pudo procesar este archivo.” Indexing/selected-set/date-order are not the blocker.
- **Real OpenCode 1.18.25 protocol (probed against bundled sidecar, isolated and production XDG):** `POST /session?directory=` → 200 top-level `{id,…}` (not `{data:{id}}`). `POST /session/{id}/prompt_async` → 204. `GET /session/{id}/message?limit=` → 200 **bare array** of `{info, parts}`. Role/id/finish under `info`. `finish` omitted while streaming, `"tool-calls"` on tool turns, `"stop"` when done. Text is `parts[].type=="text"`. `GET /session/status` is instance-scoped by `directory`; message GET is not. A K6-shaped prompt completed in ~4–8s; `OpenCodeRemoteSummarizer` against the live sidecar completed in ~5s. The `{data:[…]}` envelope is **not** this route’s contract (it is used by other list endpoints). `remote_summary_calls=0` means `summarize()` never returned `Ok` (increment is after terminal text), i.e. the 120s poll never observed usable `finish:"stop"` text — not “request never sent”.
- **Exact mismatch vs prior envelope-only fix:** that change made the fake wrap GET `/message` in `{data:[…]}` and greened parser tests, but the real 1.18.25 message list is a bare array (already accepted). Fedora still 5×120s. Remaining poll bugs: (1) completion required the **last** assistant row to be `stop`, so a trailing in-progress assistant (`parts:[]`, no finish) hid an earlier `stop`; (2) scratch sessions used `permission: []`, allowing tools/permission waits that never write `stop`; (3) timeout did not abort the scratch session; (4) conversation model was not passed on `prompt_async`.
- **Fix:** observe newest `info.finish=="stop"` with nonempty text; deny scratch tools (`external_directory` + `*`); abort on timeout/HTTP failure; pass the conversation model when known; poll `limit=1000` with `directory`; keep envelope unwrap as compatibility only. Fake GET `/message` returns a bare array again. Production `SUMMARY_TASK_TIMEOUT` remains 120s.
- **Tests:** `terminal_text_uses_newest_stop_even_if_a_later_assistant_is_still_open`; `summarize_observes_real_1_18_25_bare_array_stop_without_120s_timeout` (full summarizer poll vs sequenced FakeServer: `[]` → streaming → tool-calls → `stop` + trailing open assistant, <<120s); `summarize_times_out_when_1_18_25_never_writes_finish_stop` (abort asserted); five-file `five_staged_ready_markdown_files_generate_k6_nodes_through_opencode_envelope` still `remote_calls=7` (5 document + 1 batch + 1 global), date order, no failure copy, durable metrics, zero K6 on reopen.
- **Validation:** `cargo fmt --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS; `cargo test --workspace` PASS; frontend format/typecheck/lint/test (282) / build PASS; `git diff --check` PASS; `scripts/test-distribution-contracts` PASS; `./scripts/verify` PASS (`CI=true` for non-TTY pnpm).
- **Fresh AppImage (from HEAD `7c9327a`, Ubuntu 24.04 Podman; documented linuxdeploy → appimagetool fallback):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,144,440 bytes**, SHA-256 `34e2283d06217720fd49679a56765ab99f27ffcc7d83ddc431845d536189f011`. GLIBC max **2.39** (138 ELF). WebKitGTK relocatable runtime PASS. Graphics gate PASS. ONNX Runtime **1.22.0** payload PASS. Bundled OpenCode **1.18.25**. Bundled cloudflared **2026.8.3**. `scripts/test-distribution-contracts` PASS. Artifact not committed.
- **Gate status:** structural/regression complete. **Do not claim Fedora GUI PASS.** Windows Knowledge remains NOT YET HUMAN-VALIDATED. M11 NOT STARTED. Not pushed.
