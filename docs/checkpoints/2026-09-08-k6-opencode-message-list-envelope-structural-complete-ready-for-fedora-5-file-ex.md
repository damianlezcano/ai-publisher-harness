## K6 OPENCODE MESSAGE-LIST ENVELOPE — STRUCTURAL COMPLETE, READY FOR FEDORA 5-FILE EXHAUSTIVE-SUMMARY HUMAN VALIDATION (2026-09-08)

- **Starting tree:** `6f52581` clean except untracked local `opencode.json` (not committed). Author: Cursor Agent (`cursor-grok-4.6-high`). Independent read-only reviewer: **REVIEW=APPROVE**.
- **Human Fedora evidence (authoritative):** five composer-selected Markdown files indexed (`Archivos preparados: 5/5`, `Indexación: 5/5`, `embeddings_created=4655`), exact dated source set, no historical leakage. Assistant listed each filename with “No se pudo procesar este archivo.” Terminal: `status=completed documents=5 summary_nodes_reused=0 summary_cache_hits=0 summary_nodes_generated=0 remote_summary_calls=0`, duration ~600s, Conversation Details `provider=opencode/big-pickle`, provider tokens unavailable, **provider calls = 0**. This is **not** drag/drop, lexical, embedding-persist, selected-set cardinality, date-order, or quota failure.
- **Exact root cause:** `OpenCodeRemoteSummarizer` polled `GET /session/{id}/message` as a **bare JSON array**. OpenCode 1.18.25 list endpoints (already handled by `project-agent` / `project-provider`) return `{"location":...,"data":[...]}`. The poll never observed `finish:"stop"`, so each document node hit `SUMMARY_TASK_TIMEOUT` (120s). Five documents ≈ 600s. `summarize()` returned `ExecutionFailed` **before** `remote_calls` / `regenerated` increment, so accounting showed zeros while the remote boundary had been attempted. Ready document nodes were never stored; the selected-source surface mapped every file to the explicit failure copy.
- **Fix (smallest ownership boundary):** `session_messages_from_body` unwraps the same `data` envelope as the agent adapter (still accepts a bare array). FakeServer GET `/message` now wraps arrays with that envelope so offline tests match production. Selected-per-source inventory (`selected_material_ids` → document chunks → K6 document/batch/global) is unchanged and was not the Fedora break.
- **Accounting:** a successful five-file selected summary still plans document + batch + global nodes (measured **remote_calls=7** = 5 document + 1 batch + 1 global). Provider token fields stay null when OpenCode does not report them. Knowledge corpus metrics stay separate. “N archivos procesados” on the processing card remains **indexing** terminal state (copied/lexical/embeddings), not K6 summary success; failed summaries stay in the assistant per-source entries.
- **Tests:** `session_messages_unwrap_opencode_1_18_25_list_envelope`; production-faithful `five_staged_ready_markdown_files_generate_k6_nodes_through_opencode_envelope` (staged persist → lexical READY → deterministic embeddings → `send_summary_run_with(OpenCodeRemoteSummarizer)` vs enveloped FakeServer; five unique chunk sentinels in real prompts; historical excluded; date order; `remote_calls>0` matching executed prompts; cache reuse issues zero new prompts; reopen reads durable metrics with zero K6/index work; logs/`project.json` omit bodies). Existing CaptureSummarizer inventory tests unchanged.
- **Validation:** `cargo fmt --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS; `cargo test --workspace` PASS; frontend format/typecheck/lint/test (282) / build PASS; `git diff --check` PASS; `scripts/test-distribution-contracts` PASS; `./scripts/verify` PASS.
- **Fresh AppImage (from HEAD `606f127`, Ubuntu 24.04 Podman; documented linuxdeploy → appimagetool fallback):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,140,344 bytes**, SHA-256 `3263c163d1c85ede84561e0d0f0a0d405c5e0a965225b41af6dda6501d7c053f`. GLIBC max **2.39** (138 ELF). WebKitGTK relocatable runtime PASS. Graphics gate PASS. ONNX Runtime **1.22.0** payload PASS. Bundled OpenCode **1.18.25**. Bundled cloudflared **2026.8.3**. `scripts/test-distribution-contracts` PASS. Artifact not committed.
- **Gate status:** structural/regression complete. **Do not claim Fedora GUI PASS.** Windows Knowledge remains NOT YET HUMAN-VALIDATED. M11 NOT STARTED. Not pushed.
