## KNOWLEDGE CONTEXT ASSEMBLY PASS COMPLETE (2026-09-05)

- **K4:** K3's bounded `HybridSearchResult` output now feeds `KnowledgeStore::assemble_context` and a provider-independent structured `EvidencePackage`; neither layer constructs a prompt or changes chat/`AgentEngine`. Entries preserve source/document/chunk identity, source path/name, provenance/heading/structural metadata, hybrid scores/ranks, exact-ID/lexical/semantic/neighbor signals, embedding generation, excerpt status, and deterministic estimated cost. It is ephemeral; schema and dependencies are unchanged.
- **Budget and selection:** evidence-only default ceiling is 3,000 conservative local units with a 300-unit reserve (usable 2,700). The estimator is deliberately independent of E5 and uses `ceil(UTF-8 bytes / 3)` plus 24 fixed entry-framing units. Every output proves `estimated_budget_used <= estimated_budget_limit`. Defaults cap eight entries, two/document, two/source, 900 units/entry, ten K3 candidates, and require a 64-unit useful minimum. Direct K3 rank order wins; duplicate chunks/canonical text, document/source caps, and maximum entry size are enforced deterministically.
- **Neighbors/truncation:** same-document ordinal neighbors at radius one are lower priority than every direct match, spend normal budget/caps, preserve provenance, and are marked `neighbor_of`. Safe UTF-8 prefix excerpts prefer structural boundaries, explicitly carry `TruncatedExcerpt`, and narrow end offsets/lines. Empty and lexical-only K3 results form valid packages. CONTEXT ASSEMBLY REMOTE LLM CALLS: ZERO.
- **Validation:** 21 focused project-knowledge tests cover hard/exact/tight budgets, maximum entry size, diversity, canonical/chunk dedupe, provenance, UTF-8 truncation, neighbor rules, deterministic ordering, empty/lexical-only packages, project isolation, Spanish OpenShift evidence, exact `INC-12345`, multi-source selection, and constrained budget behavior. K1, K2, and K3 remain intact. **CHAT / LLM INJECTION NOT STARTED. M11: NOT STARTED.**
