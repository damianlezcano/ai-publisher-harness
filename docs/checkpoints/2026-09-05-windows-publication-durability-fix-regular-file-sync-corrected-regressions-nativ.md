## WINDOWS PUBLICATION DURABILITY FIX — regular-file sync corrected + regressions + native rebuild/install/runtime baseline; share retest BLOCKED by unavailable Windows UI automation (2026-09-05)

- **Scope:** bounded Windows publication compatibility only. The existing false-`PathEscape` correction was preserved exactly: `canonical_project_dir_matches` accepts the owning project's canonical/verbatim Windows path and rejects another project's canonical path; Unix continues to require exact equality. The pre-existing lifecycle fixture now compares against the canonical owning directory so it remains valid on Windows.
- **Proven root cause fixed:** `fsync_tree` opened staged regular files with read-only `File::open`, then called `sync_all`; Windows rejects that flush with `ERROR_ACCESS_DENIED` / raw OS error 5. `crates/project-fs/src/publication_snapshot.rs` now opens only existing staged regular files through `OpenOptions::new().read(true).write(true).open(&p)` before the unchanged `sync_all`. It sets no create, truncate, or append flag; contents cannot be changed by this handle. Any open or `sync_all` failure still maps fail-closed to `ProjectCoreError::WriteFailed`. `sync_parent_dir` and all directory behavior are untouched.
- **Native-Windows regressions:** new `publication_snapshot_syncs_nested_web_files_without_mutating_bytes` creates a fresh project and public web Creation with `index.html`, `app.js`, and `assets/small.bin`, prepares/installs the snapshot, and asserts all three published byte sequences exactly. It passes on Windows and exercises nested regular-file durability; it fails against the old read-only handle at `fsync_tree`. `canonical_project_dir_accepts_verbatim_equivalent_and_rejects_other_project` PASS; full `project_lifecycle` 75/75 PASS; `publication_snapshot_security` 7/7 PASS. `publication_snapshot` is 8/10: two pre-existing Windows fixture assertions expect slash-form generated links while this host emits backslashes; link generation is untouched by this pass.
- **Checks:** `cargo fmt --all -- --check` PASS; `cargo clippy -p project-fs --all-targets -- -D warnings` PASS; `cargo test -p project-fs --lib` PASS; focused Windows durability test PASS; `cargo test -p project-fs --test project_lifecycle` PASS (75/75); `cargo test -p project-fs --test publication_snapshot_security` PASS (7/7); `git diff --check` PASS. `./scripts/verify` remains not runnable on this Windows host because its bash/Linux/WebKit gate is unavailable.
- **Build/install/runtime:** `powershell -ExecutionPolicy Bypass -File .\\packaging\\windows\\build.ps1` produced `app/src-tauri/target/release/bundle/nsis/EducAI_0.1.0_x64-setup.exe`, **59,253,392 bytes**, timestamp **2026-09-05T09:20:13-03:00**, SHA-256 **ED16B78511E7ECA42B8E4FD15B52EFCF5C4F1783AA857D3286E3B3F1396B7134**. Silent NSIS update preserved `%APPDATA%\\com.educai.publisher`, including project `01a06f16-6abe-7102-9bba-8f72e986f0a2`, its persisted conversation, and public web Creation `Actividad`. Installed `educai.exe --debug` launched; its owned `opencode.exe` child listened on `127.0.0.1:51195`; `GET /global/health` returned HTTP 200 with version 1.18.25.
- **Share retest blocker (one new operational blocker):** the available Windows UI automation helper exited before target selection with `windows sandbox failed: helper_unknown_error: apply deny-read ACLs`. Therefore neither Preview nor the one required `Compartir` click could be performed, and the share pipeline stages (source resolution, staging, install/rename, local publish/HTTP, cloudflared, tunnel/public HTTPS, and Stop sharing) are **NOT REACHED**. No product defect after the fixed regular-file sync was observed, and no further implementation work was started.
- **M11 NOT STARTED. OBSERVABILITY PARTIAL — DEFERRED.** Next step: restore a targetable Windows UI automation surface or have the human perform exactly one `Compartir` click on the persisted `Actividad` Creation, then capture the requested share-stage results and Stop-sharing proof.
