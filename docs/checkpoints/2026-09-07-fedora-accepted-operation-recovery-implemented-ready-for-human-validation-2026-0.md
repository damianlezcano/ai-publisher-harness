## FEDORA ACCEPTED-OPERATION RECOVERY — IMPLEMENTED, READY FOR HUMAN VALIDATION (2026-09-07)

- **Starting tree:** `5a962b0` (clean). A real Fedora human test exposed the exact remaining bug: after a 52-file accepted Send, the durable progress showed `prepared=52 / indexed=0 / embeddings=0 / ready=0`, EducAI printed the generic fatal "Algo salió mal, reiniciá la aplicación", and reopening the AppImage reconstructed the conversation/operation/progress correctly but **did not resume local processing**. Root cause: the accepted-operation resume API and idempotent local pipeline already existed, but **no reopen trigger/seam ever invoked them** — `open_project` only rebuilt the read model while the auto-resume boundary silently refused `PendingRetry` and the frontend had no typed state and only emitted the generic fatal copy (which collapsed the real `pending_retry`/`copying` state).
- **Fix (bounded, no background worker):** `AcceptedImportProgressView` now exposes `operation_id` + `agent_state`; `App` auto-resumes (once per operation) an incomplete `agent_state == not_started` operation via a new `agent_resume_import` Tauri command; `resume_accepted_import_operation` now gates the remote boundary strictly on `agent_state` (`not_started` continues; `started_outcome_unknown`/`completed`/`failed_*` are never auto-resent) while allowing a `pending_retry`+`not_started` local interruption to resume. Recovery logs emit sanitized `[knowledge][recovery]` events (discovered / started / `phase=lexical` / `phase=embeddings` / completed / typed `failure_class`); failures classify `durable_state_read_failed`, `remote_outcome_unknown`, `inference_failed`, `other_typed_local_failure` without document/chunk/prompt/path/vector content.
- **UX:** the progress card keeps its structure but is now state-driven; `pending_retry|not_started` shows "El procesamiento quedó pendiente. Podés reintentarlo." with a Reintentar action; `started_outcome_unknown` shows truthful outcome-unknown copy — the generic fatal "Algo salió mal, reiniciá la aplicación" is no longer reachable from a durably recoverable/core state. Chat-side invariant preserved: pending/staged attachments are never history; composer stays clear after an accepted Send.
- **Tests:** backend `fifty_two_file_accepted_operation_resumes_after_reopen_without_duplication`, `repeated_restart_recovery_is_idempotent_and_reuses_ready_work`, `pending_retry_not_started_is_locally_resumable_but_remote_unknown_is_not` (plus the existing 51-file/reopen/outcome-unknown coverage); frontend auto-resume + typed-copy + Reintentar + no-duplicate-turn coverage (269 tests). `cargo fmt --check`, `clippy -D warnings` (knowledge/app/agent), `cargo test --workspace` (all green), frontend `typecheck`/`lint`/`format:check`/`test`/`build` green. `git diff --check` PASS. `./scripts/verify` reaches the workspace strict-Clippy gate and stops ONLY on the two documented pre-existing `unused_mut` warnings in unchanged `project-tunnel`/`project-opencode` lib-test code.
- **Fresh AppImage:** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,074,808 bytes**, **SHA-256 `1af74f0507f7995ecf35746b00e101b87b0767d059a9f2b9816476d9d59a15bd`**. Bundled `opencode` + `cloudflared` + ONNX Runtime (model/tokenizer external); `check-appimage-glibc` (138 ELF ≤2.39), WebKitGTK, and ONNX Runtime distribution gates PASS.
- **Gate status:** automated/backend/frontend/distribution checks green; the artifact has **not** yet received the graphical Fedora human re-test (drag 52 files, type "haceme un resumen de cada archivo ordenado por fecha", Enviar, close mid-progress, reopen `--debug`, confirm local resume advances past 0/52 with no duplicate turn/Materials, verify `[knowledge][recovery]` logs and typed `failure_class`). **READY FOR FEDORA HUMAN VALIDATION.** Windows Knowledge remains NOT YET HUMAN-VALIDATED; M11 NOT STARTED; the older long-run `inference_failed` runtime issue is out of scope until this recovery gate is human-pass.
