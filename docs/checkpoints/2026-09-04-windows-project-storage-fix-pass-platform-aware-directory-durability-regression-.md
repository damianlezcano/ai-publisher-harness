## WINDOWS PROJECT STORAGE FIX PASS — platform-aware directory durability + regression + native NSIS rebuild + installed runtime persistence proof (2026-09-04)

- **Scope:** bounded Windows project-storage durability correction only. **Proven root cause (do NOT reopen):** project creation wrote `project.json` through the existing staged atomic-write path, then tried to open the staging directory with ordinary `std::fs::File::open` for directory sync. Windows rejects that ordinary file-style directory open with access denied, causing `ProjectCoreError::WriteFailed` before the first project could be committed.
- **Implementation:** `crates/project-fs/src/lib.rs` introduces one `sync_parent_dir` boundary. Unix retains the prior `File::open(directory).sync_all()` behavior and propagates failures. Windows explicitly returns success only for the unsupported parent-directory durability step: file content still calls `sync_all()` before rename, and temp writes, atomic persist/rename, containment checks, and all file-write/rename failures remain fail-closed. Windows has no POSIX-equivalent directory fsync exposed by the supported Win32 file flush API; this is documented in code rather than guessed or silently swallowed. `publication_snapshot.rs` now uses that same narrow boundary, preserving its Unix directory-sync semantics.
- **Regression:** `windows_first_project_creation_persists_across_reopen` in `crates/project-fs/tests/project_lifecycle.rs` proves empty repository → create first project → `project.json` exists → list returns it → fresh repository instance reads/lists it. It passed on native Windows and fails against the prior ordinary-directory-open implementation.
- **Focused validation:** `cargo fmt --all -- --check` PASS; `cargo clippy -p project-fs --all-targets -- -D warnings` PASS; Windows regression PASS; `cargo test -p project-app --test app_facade project_lifecycle` PASS; `git diff --check` PASS. The full `project-fs` lifecycle suite remains 72/74 on this Windows host, with two pre-existing unrelated Windows path/canonicalization fixture failures; snapshot suites similarly fail pre-existing `PathEscape` cases. `./scripts/verify` could not run because this native Windows host has no `bash`; its Linux/WebKit gate is not runnable here.
- **Native build / install / runtime proof:** rebuilt `app/src-tauri/target/release/bundle/nsis/EducAI_0.1.0_x64-setup.exe`, **59,231,950 bytes**, SHA-256 **FCCA21AB4FFF26CC44D2FBE8E155B92C1A24D4D99A8D8ECBAC7DB4806EADF42F**; silent NSIS update installed it without clearing existing app data. First launch created `Conversación nueva` (`01a06f16-6abe-7102-9bba-8f72e986f0a2`), persisted `C:\\Users\\lezca\\AppData\\Roaming\\com.educai.publisher\\projects\\01a06f16-6abe-7102-9bba-8f72e986f0a2\\project.json`, and logged OpenCode ready (1.18.25). Child owned by EducAI listened on loopback; `/global/health` returned HTTP 200 after restart. Normal close stopped the owned child on both runs. Restart retained exactly the one project and did not create another.
- **UI-surface caveat:** the available Windows automation surface exposes no native app target, so visual confirmation of sidebar rendering and absence of the exact Spanish startup toasts could not be captured; backend logs show successful automatic project creation with no project-create error. **SECONDARY OBSERVABILITY GAP — DEFERRED.**
- **M11 remains NOT STARTED.** Chat, attachments, Creations, Preview, Cloudflare, OpenCode environment behavior, project layout, and logging are untouched. **Next step: RESUME WINDOWS HUMAN RUNTIME VALIDATION — CHAT ROUND-TRIP NEXT.**
