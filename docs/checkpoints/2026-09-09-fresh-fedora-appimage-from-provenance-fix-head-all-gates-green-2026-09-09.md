## FRESH FEDORA APPIMAGE FROM PROVENANCE-FIX HEAD — ALL GATES GREEN (2026-09-09)

- **Starting/ending HEAD:** `6a5d58cb9cabc93e481fd1d5642a1e89c7db2cf1` `fix(knowledge): cite only selected supporting evidence as Fuentes` (source-provenance correction; independently reviewed, **REVIEW=APPROVE**). Working tree clean except untracked local `opencode.json` (not committed). AppImage **not committed**; only this docs checkpoint commit follows the artifact.
- **Objective:** fresh bounded certification build from the provenance-fix HEAD in the pinned Ubuntu 24.04 container root, then re-validate the produced AppImage artifact. No Windows work, no M11, no push, no redesign, no human PASS claimed.
- **Artifact (built, not committed):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,189,496 bytes**, SHA-256 `ad4d15c2e761b74b030263e16eaab6178fd683490b2d88ee33f6f76a45593a81`, mtime `2026-09-09 10:30:17 -0300` (build postdates commit `6a5d58c` `2026-09-09 10:18:35`).
- **Build path:** `./scripts/package linux-appimage` (podman, image `educai-linux-portable:ubuntu-24.04`). First attempt aborted before bundling on a transient HTTP 503 downloading a tauri linuxdeploy plugin; retry proceeded. tauri `linuxdeploy` then aborted on the pinned static sidecars (documented ldd behavior) and the documented `appimagetool` fallback packaged the container-assembled AppDir. Release compile in container: 42.92s incremental.
- **Artifact gates (fresh AppImage + extracted squashfs root, re-run on host):** `check-appimage-glibc` PASS — 138 inspected ELFs, all ≤ GLIBC 2.39 (measured max **GLIBC_2.39**); `check-appimage-webkit-runtime` / `check-webkit-appdir` PASS — relocatable WebKitGTK 4.1 runtime under `usr/lib/x86_64-linux-gnu/webkit2gtk-4.1`; `check-graphics-appdir` PASS — host Wayland/EGL/GLVND/Mesa/GBM/DRM/DRI boundary preserved; `check-appimage-onnxruntime` PASS — `libonnxruntime.so.1.22.0` 21,042,416 bytes sha256 `3da6146e…35c673` (max GLIBC_2.27), `libonnxruntime_providers_shared.so` 14,632 bytes sha256 `e4706ea0…a8451` (max GLIBC_2.2), required symlinks intact.
- **Bundled sidecars (from extracted AppDir):** OpenCode **1.18.25**, cloudflared **2026.8.3** (built 2026-08-31), ONNX Runtime **1.22.0** — matches pinned sidecar set.
- **Repository gates (current tree):** `cargo fmt --all -- --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS (0 warnings); `cargo test --workspace` PASS — **66 test binaries, 0 failures**; focused `exhaustive_rag` suite **15/15** including provenance contracts `g_source_traceability_has_filename_without_absolute_path`, `duplicate_chunks_from_one_source_emit_one_fuente`, `five_document_corpus_does_not_emit_five_fuentes`, `budget_dropped_exhaustive_sources_are_not_fuentes`, `h_privacy_metrics_and_logs_omit_bodies_paths_and_secrets`, and restart/reopen `k_and_l_restart_durability_and_zero_work_reopen`. Frontend (app/): `pnpm install --frozen-lockfile` up to date, `format:check` PASS, `typecheck` PASS, `lint` PASS, `test` **21 files / 282 tests PASS**, production `build` PASS. `git diff --check` clean; `./scripts/test-distribution-contracts` PASS; `./scripts/verify` PASS (M0 harness, M10 0.1.0 alignment, UX_REDESIGN_01, Tauri cargo check).
- **Carried semantic behavior:** normal compact K3/K4 top-k retrieval unchanged; K6 selected-source per-source failures explicit; exhaustive question routes to local whole-corpus READY-chunk inspection; exhaustive negative is local/complete and issues no per-document remote calls; Fuentes are emitted only from selected supporting evidence (filename without absolute path, no fake source names, no secrets); durable TurnMetrics/Conversation Details reopen causes zero new provider/index/embedding/retrieval/K6 work.
