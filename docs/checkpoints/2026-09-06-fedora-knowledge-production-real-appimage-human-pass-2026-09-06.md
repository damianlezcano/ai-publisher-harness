## FEDORA KNOWLEDGE PRODUCTION / REAL APPIMAGE: HUMAN-PASS (2026-09-06)

- **Gate closed:** `FEDORA KNOWLEDGE PRODUCTION REAL-APPIMAGE GATE CLOSED — HUMAN-PASS`. The real AppImage + real remote (billed) model + real bundled ONNX Runtime + real verified model cache were exercised end-to-end on Fedora against a synthetic 51-document meeting-note corpus. This was a validation gate; the only product change was one bounded observability fix (below). No architecture refactor. **Windows Knowledge runtime remains NOT YET HUMAN-VALIDATED. M11: NOT STARTED.**
- **Fresh AppImage built from HEAD `51926a0`:** `./scripts/package linux-appimage` (Ubuntu 24.04 podman) → `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,013,368 bytes**, **SHA-256 `55a6dd4c0a8cd627d51992e6253568d2aa705c26c0737d2818e8a15eac24ec42`**. Distribution gates inside the build PASS: `check-appimage-glibc` (138 ELF all ≤ GLIBC 2.39), `check-appimage-webkit-runtime`, `check-graphics-appdir`, `check-appimage-onnxruntime` (packaged `usr/lib/educai/onnxruntime/libonnxruntime.so.1.22.0` + required symlinks, GLIBC core 2.27 / provider 2.2). Artifact verified to contain `educai` + bundled `opencode` (1.18.25) + bundled `cloudflared` + bundled ONNX Runtime; the E5 model/tokenizer stay external in the app-data model cache (model.onnx is NOT in the AppImage; `educai` DT_NEEDED has no libonnxruntime — dlopen-only; no host ONNX Runtime in the ldconfig cache). `scripts/test-distribution-contracts` PASS. The AppImage was not committed.
- **Real AppImage startup (Section C):** the freshly built AppImage launched on the live Fedora session with `--debug`; the bundled `opencode serve --hostname 127.0.0.1 --port … --pure` child reached `GET /global/health` → **HTTP 200 `{"healthy":true,"version":"1.18.25"}`**. Clean TERM shutdown stopped the owned OpenCode child with no residue. `runtime_library_from_executable(current_exe)` resolved the **packaged** `usr/lib/educai/onnxruntime/libonnxruntime.so` from a harness binary placed in the extracted AppDir `usr/bin/` (no cwd, PATH, or ORT-env dependence), matching the documented K5 `educai-runtime-gate` pattern.
- **51-document synthetic corpus (Section D/F):** 51 non-sensitive Markdown meeting notes (Spanish/English, cross-document threads: OpenShift/automation decisions, INC-12345-style exact identifiers, security/MFA, deployment, completed/pending decisions) imported through the production `AppState`/`import_materials` path. Ingestion result: **materials_total=51, ready=51, pending=0, failed=0, unsupported=0; chunks_total=612, embeddings_ready=612** (all real local E5 embeddings generated from the packaged ONNX Runtime path). No private transcripts used; the operator's real meeting notes were never read or ingested.
- **Local E5/ORT (Section G):** `semantic_provider_state=available`. Proof: bundled ONNX Runtime loaded from the packaged path, verified model cache used (`ModelManager::inspect == Verified`), tokenizer loaded, real 384-dim normalized E5 vectors (612 document + query embeddings), no remote embedding service. Retrieval log per turn reported `semantic=available semantic_state=available`.
- **Ordinary real knowledge query (Section H):** `"¿Qué se decidió sobre la automatización de OpenShift y qué quedó pendiente?"` through the real model: `retrieval_mode=hybrid, lexical_candidates=0, semantic_candidates=40, fused_candidates=10, selected_evidence_count=8, context_budget_limit=2700, context_budget_used=374, evidence_bytes=534, evidence_utf8_chars=518, evidence_est_tokens=374, request_preparation_ms=36`. Answer completed via the real `opencode-go/deepseek-v4-flash` model in 16.4 s.
- **Raw-document boundary proof (Section I):** the recorded request boundary for the ordinary query contained the bounded `<knowledge_evidence trust="untrusted">` block with 8 evidence entries and **no raw corpus marker, no absolute `inputs/` path, no `knowledge.sqlite`, no raw workspace attachment** (prompt bytes 3717). The production dedup (`project-agent/src/service.rs` filters indexed source names out of raw workspace attachments) is exercised and structurally confirmed by the committed RecordingEngine tests. The final boundary is `51 project materials → local index → K3 hybrid retrieval → K4 bounded evidence → remote LLM`, NOT `51 full raw files → remote LLM`.
- **K6 whole-project summary first run (Section J):** `summarize_project` over the 51-doc corpus → **`remote_calls=54, estimated_input_units=18419, hierarchy_depth=3, source_count=51`, duration 935 s** (51 document summaries + deterministic batches + global). No request carried the corpus; batch/global requests were bounded (≤ branching factor, labelled `P1..Pn`). **FIRST RUN node accounting measured:** during the run, 5 document nodes failed K6 structured-output validation (`invalid_output`) with 1 downstream `empty_corpus` — natural DeepSeek V4 Flash nondeterminism, never a fake global. Bounded retries recovered them: a retry harness re-ran `summarize_project` → round 1 `remote_calls=1 cache_hits=55 reused=55`, round 2 `remote_calls=4 regenerated=4` → **ALL 59 nodes Ready (51 document + 7 batch + 1 global)**.
- **K6 cache reuse (Section K):** the identical whole-project summary request immediately after the first run returned `remote_calls=7, cache_hits=49, reused=49, regenerated=6` (the 6 = the failed nodes retried; 49 Ready nodes reused). After retries settled all 59 Ready, a final identical request returned **`remote_calls=0, cache_hits=59, reused=59, regenerated=0`** — full cache hit, no remote spend.
- **Quantitative context savings (Section L, architectural estimates, not exact provider savings):** CORPUS materials=51, naive_corpus_est_tokens=8,229 (corrected, see fix below). ORDINARY QUERY evidence_est_tokens=374 → **context_reduction_pct_vs_naive_corpus=95.5%**. GLOBAL SUMMARY FIRST RUN remote_calls=54, estimated_input_units=18,419 (summed per-node estimates), estimated_reduction vs a hypothetical single 51-doc prompt ≫ 98% (the corpus was never sent). GLOBAL SUMMARY SECOND RUN (post-settle) remote_calls=0, cache_hits=59, reused=59. ACTUAL provider token telemetry (opencode `serve` session accounting for the K6 scratch sessions) recorded **input=302,359 / output=113,852 / cache_read=468,736 / cost≈$0.166** for the paid summary sessions and input=8,287 / output=708 / cost≈$0.0025 for the ordinary query + restart query — labelled ACTUAL, kept separate from ESTIMATED values.
- **Remote-call accounting (Section M):** DOCUMENT INGESTION / EMBEDDING / QUERY EMBEDDING / RETRIEVAL / CONTEXT ASSEMBLY REMOTE LLM CALLS: **ZERO** (all local E5/ONNX/SQLite). SUMMARY REMOTE LLM CALLS: 54 (first) / 7 (immediate second, retrying 6 failed nodes) / 0 (settled cache hit). FINAL CHAT REMOTE LLM CALLS: one per real user turn. No remote embedding, retrieval, or context-assembly stage exists.
- **Timeout classification (Section N):** the committed `AgentError::Timeout`/`task_failure_kind()=timeout` path was verified structurally (FakeServer bounded-timeout tests PASS) and no forced 120 s timeout was induced. The natural K6 node failures observed were classified `invalid_output`/`empty_corpus` (not opaque "task failed / No se pudo completar la creación"); both recovered by bounded retry.
- **Mass-attachment UI (Section E):** committed `ComposerBar` tests (18) PASS including the exact 51-attachment case: composer + Enviar button stay visible, 51 chips collapse to a truthful `"51 archivos seleccionados · Ver todos"` toggle, expand/collapse works. Truthful duplicate copy verified in committed code/tests: pre-existing → `duplicate` → "ya estaba en el proyecto"; same-batch → `duplicate_in_batch` → "ya estaba en esta selección" (a same-batch duplicate is never described as already-in-project). Accepted project materials render distinct from pending current-turn attachments (`WorkspaceView` separates the two sets). Frontend suite: **245 tests PASS (21 files)**; `tsc --noEmit`, `eslint`, `prettier --check`, `vite build` all PASS.
- **Restart/persistence (Section P):** reopening the same validation app-data in a fresh process (simulating an EducAI restart) verified: project persists (51 materials), Knowledge DB persists, **Ready statuses persist (51/51 materials, 59/59 summaries)**, model cache stays `Verified` (no re-download), **unchanged documents are not re-embedded (`embedded=0, reused=612`)**, summaries/cache remain reusable (`remote_calls=0` full cache hit), and one small Knowledge query after restart completed via the real model (`semantic=available`, evidence=5, `context_reduction_pct_vs_naive_corpus=97.7%`). Clean shutdown confirmed (owned OpenCode child stopped, no residue).
- **Regression (Section Q):** normal chat, creation, Preview/Abrir, Share, Stop Share all covered by the passing workspace/frontend suites (chat/facade tests, `preview.rs`, publication + tunnel tests incl. `drop_cleans_up_running_tunnel`). No unrelated UX expansion.
- **Bounded observability fix (this gate):** the real run surfaced that `KnowledgeStore::corpus_stats()` computed `corpus_bytes` through a documents-chunks `LEFT JOIN`, so `SUM(d.byte_size)` was multiplied by each document's chunk multiplicity — inflating `corpus_bytes`, `naive_corpus_est_tokens`, and `context_reduction_pct_vs_naive_corpus` (12× on this corpus). Bounded mechanical fix `316762a`: compute `SUM(byte_size)` from `documents` and `SUM(LENGTH(text))` from `chunks` separately, plus a regression test (`corpus_bytes_counts_each_document_once_not_per_chunk`) pinning `corpus_bytes` to the source byte count. Corrected values: `corpus_bytes=24,685, corpus_utf8_chars=23,394, naive_corpus_est_tokens=8,229`.
- **Validation (Section R):** `cargo fmt --all -- --check` PASS; `cargo clippy -p project-knowledge/-project-app/-project-agent --all-targets -- -D warnings` PASS (36 / app incl. 13 knowledge + 12 summarization + 21 materials / agent 29); `cargo test -p project-knowledge` (36), `-p project-app`, `-p project-agent` (29), `cargo test --workspace` PASS (64 test binaries, 0 failures); frontend 245 tests + typecheck/lint/format/build PASS; `git diff --check` PASS; `scripts/test-distribution-contracts` PASS. `./scripts/verify` reaches the workspace strict-Clippy gate and stops on the SAME two pre-existing `unused_mut` warnings in **unchanged** `project-tunnel/src/cloudflare.rs:291` and `project-opencode/src/lib.rs:154` (lib-test), reported unchanged, not broadened.
- **Privacy (Section O):** all gate/session logs carry only structural metadata: ids, counts, bytes, token estimates, timings, provider/model, status enums, failure classes, summary/cache accounting. No document bodies, chunk text, query text, assistant text, vectors, API keys, credentials, raw provider payloads, or private absolute paths. The temporary runtime harnesses used for the billed runs were removed after the run (no files remain in the tree); the AppImage, model cache, generated corpus, and logs were not committed.
- **Remaining gates:** **Windows Knowledge runtime: NOT YET HUMAN-VALIDATED.** **M11: NOT STARTED.** Next gates: Windows Knowledge runtime human validation, then M11.
