## EXHAUSTIVE-RAG BUILD CERTIFICATION — EXISTING FEDORA APPIMAGE PROVEN FROM COMMITTED HEAD, ALL GATES RE-RUN (2026-09-09)

- **Starting tree:** `2f0f9fd8bf54aa073308d808c87b84a70400e0d6` clean except untracked local `opencode.json` (not committed). Author: opencode. Independent read-only review: **REVIEW=APPROVE** carried over from the exhaustive-RAG diff.
- **Objective:** certify (no rebuild) that the existing Fedora AppImage was built from committed HEAD containing the exhaustive-RAG commits `dd89496` (presence scan over READY chunks), `4e215d2` (corpus-wide questions route to exhaustive local inspection), `2f0f9fd` (exhaustive RAG contracts + QA split); re-run all mandatory repository/distribution gates. No Windows work, no M11, no push, no human PASS claimed.
- **Artifact (certified, not committed):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,201,784 bytes**, SHA-256 `4e7029dfa6a8bfab0bd1357cd08f33de554b112f8f182595b60a5677c32e0823`. Mtime `2026-09-08 23:56:03 -0300`; release binary `app/src-tauri/target/release/educai` mtime `2026-09-08 23:55:38 -0300`, both after final commit `2f0f9fd` (`2026-09-08 23:54:04`) — packaging postdates the entire exhaustive-RAG commit range, so the AppImage contains the work.
- **Distribution/AppImage gates (against the existing artifact + extracted squashfs root):** `check-appimage-glibc` PASS (138 ELFs, all ≤ GLIBC 2.39); `check-appimage-onnxruntime` PASS (`libonnxruntime.so.1.22.0` 21,042,416 bytes sha256 `3da6146e…35c673`, `libonnxruntime_providers_shared.so` 14,632 bytes sha256 `e4706ea0…a8451`, required symlinks, GLIBC max 2.27/2.2); `check-appimage-webkit-runtime` PASS (relocatable WebKitGTK 4.1 runtime under `usr/lib/x86_64-linux-gnu/webkit2gtk-4.1`); `check-graphics-appdir` PASS (no host-driver-facing graphics layer in payload); `fetch-sidecars --check` PASS (pinned opencode **1.18.25**, cloudflared **2026.8.3**, onnxruntime **1.22.0**); `test-distribution-contracts` PASS.
- **Repository gates:** `CI=true ./scripts/verify` PASS (exit 0; log `/tmp/opencode/verify.log`), covering `M0 harness contract`, `scripts/test-agent-launch`, `scripts/test-session-budget`, `cargo fmt --all -- --check`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo test --locked --workspace --all-targets` (all suites ok, including the 12-test exhaustive-RAG contract suite A–L and all prior K6/TurnMetrics/lifecycle regressions), `M10 version alignment: 0.1.0`, `UX_REDESIGN_01 contract`, Tauri `cargo check`, and `git diff --check`. Separate explicit gates also PASS: frontend `format:check` / `lint` / `typecheck` / `test` (**282/282**) and `pnpm --dir app run build` (`tsc --noEmit` + vite production build).
- **Carried semantic behavior (unchanged this pass):** normal questions remain compact top-k (K3/K4); K6 selected-source per-source failures remain explicit; corpus-wide questions route to exhaustive READY-chunk inspection; durable TurnMetrics / Conversation Details reopen causes zero new provider/index/embedding/retrieval/K6 work.
- **Gate status:** build provenance + distribution + structural/regression gates complete. **Do not claim Fedora GUI HUMAN-PASS.** Windows remains NOT YET HUMAN-VALIDATED (existing Windows features HUMAN-PASS; Windows Knowledge runtime pending). M11 NOT STARTED. NOT PUSHED.
- **Handoff status:** this checkpoint entry is committed as part of the certification close (working tree remains clean except untracked local `opencode.json`); no push performed. Certification pass only: the canonical packaging path (`./scripts/package linux-appimage`, Ubuntu 24.04 Podman) was **not** re-executed; provenance rests on artifact mtime/SHA-256 vs committed HEAD, not on a fresh controlled rebuild.
