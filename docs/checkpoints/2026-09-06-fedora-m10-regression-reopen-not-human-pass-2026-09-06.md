## FEDORA M10 REGRESSION REOPEN — NOT HUMAN-PASS (2026-09-06)

- **Starting head:** `a0d8ee5`. A fresh Fedora `scripts/smoke-package appimage` artifact was proven invalid: its AppDir was assembled from Fedora host libraries and lacked the bundled ONNX Runtime payload. The retained 51-material project database therefore records `ready=51`, `chunks_total=31,876`, and `embeddings_ready=0`; the per-material warning was caused by local runtime resolution, not by model corruption or remote inference.
- **Cause and bounded repair:** `scripts/smoke-package` now calls only `scripts/package linux-appimage` for AppImage output, which builds in the controlled Ubuntu 24.04 Podman root and gates GLIBC, WebKitGTK, and ONNX Runtime in the final artifact. The smoke path no longer calls Fedora `cargo tauri build --bundles appimage` or seals a Fedora AppDir.
- **Current controlled artifact:** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, 159,001,080 bytes, SHA-256 `01ed5e0d90d145dad31ff07722d7b90c74a455c241f8ce86b92e206196fe717c`. `check-appimage-glibc` PASS (138 ELF, <=2.39); WebKitGTK and ONNX Runtime payload gates PASS. Extracted bundled ORT plus the verified cache completed real local E5 inference (`dimension=384`, normalized output); startup/shutdown spawned and stopped bundled OpenCode 1.18.25 cleanly.
- **Import UI repair:** batch results now live in the scrollable timeline as a single summary with collapsed `Ver detalle`; individual status rows render only after deliberate expansion, are bounded to 25vh, and cannot consume the fixed composer region. Tests cover 1, 5, 8, 9, 16, 51, and 100 materials plus truthful pre-existing vs same-selection duplicate detail.
- **Semantic observability repair:** `embedding_index_failed` now includes a sanitized `failure_class`. Provider/load classifications include `runtime_not_found`, `runtime_load_failed`, `model_not_installed`, `model_incomplete`, `model_corrupt`, `tokenizer_load_failed`, `inference_failed`, `embedding_persist_failed`, and `other_typed_local_failure`; no error body, path, source content, prompt, vector, or remote fallback is emitted. Materials remain durable and lexical indexing remains available.
- **Gate status:** automated/package/runtime checks are passing, but the new artifact has **not** yet received the full graphical 51-document operator retest (UI interaction, semantic query, K6 routing, usage/estimate log inspection, and restart). Do not claim Fedora HUMAN-PASS. Windows unchanged; M11 NOT STARTED.
