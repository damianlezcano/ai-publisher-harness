## FEDORA RECOVERY-TRIGGER INSTRUMENTATION PASS — TRACE ADDED, NOT HUMAN-PASS (2026-09-07)

- **Starting tree:** `3a66045` (clean). A fresh real Fedora test of the previously-built AppImage still did **not** resume: after a 52-file accepted Send the durable progress reconstructed correctly (`prepared=52 / indexed=0 / embeddings=0 / ready=0`, no duplicate turn/material, composer clean) but **no recovery ran** and no `[knowledge][recovery]` evidence appeared under `--debug`.
- **Static trace result:** every link in the reopen chain was found **present and correct** in the current tree — the frontend `App` reopen effect (`agent_state == not_started` → `agent_resume_import` once per operation), the `agent_resume_import` Tauri command (registered in `generate_handler!`, camelCase `projectId`/`operationId` → `project_id`/`operation_id`), the `resume_accepted_import_operation` eligibility gate, and the idempotent local pipeline. The production `dist` bundle also contains the trigger. Backend `52-file`/`repeated_restart`/`pending_retry`/`outcome_unknown` tests and the frontend auto-resume tests all pass. Because the real runtime failure could not be reproduced statically, this pass converted the trigger into a **fully instrumented, provable** chain and fixed the one genuinely broken link in the UX surface.
- **Instrumentation (§4):** every recovery boundary now emits sanitized structural logs with the opaque `operation_id` + counters + enum states (never document/chunk/prompt/path/vector content). Frontend `console.info/error` `[recovery-ui]` (detected / invoking / accepted / failed + `failure_class`); backend `stderr` `[knowledge][recovery]` `command_received` → `resume_requested` → `operation_loaded` → `resume_decision=allowed|denied reason=…` → `local_resume_started` → `lexical_progress`/`embedding_progress` → `local_resume_completed`/`local_resume_failed`. This pins the FIRST broken link on the next real run.
- **UX fix (§10/§11):** an auto-resume invoke failure was previously swallowed silently (`.catch` deleted the dedup key with no UI), leaving the card frozen at `Procesando 52 archivos / Indexación 0/52` with no explanation — exactly the operator's symptom. Now a safe-local resume failure surfaces the typed copy **"No pudimos reanudar el procesamiento local."** with a `Reintentar` button that re-invokes the SAME `agent_resume_import` path with the SAME `operation_id` (no new turn/material/remote resend). `started_outcome_unknown` still shows the truthful outcome-unknown copy (auto-retry blocked) and never auto-resends.
- **Tests:** frontend 276 (added: rerender idempotency, already-complete no-op, started-outcome-unknown/failed-terminal no-op, missing operation-id no-op, typed resume-failure + retry, WorkspaceView resume-failure copy + retry); backend unchanged and green. `cargo fmt --check`, `clippy -D warnings` (knowledge/app/agent), `cargo test --workspace`, frontend `typecheck`/`lint`/`format:check`/`test`/`build` all green; `git diff --check` PASS. `./scripts/verify` stops ONLY on the two documented pre-existing Windows-only `unused_mut` findings in unchanged `project-tunnel`/`project-opencode` lib-test code.
- **Gate status:** NOT HUMAN-PASS. No AppImage was produced this pass (per §21: the real frontend→Tauri chain has not yet been observed end-to-end). The next Fedora run of the checklist with `--debug` must show, in order, `[recovery-ui] incomplete operation detected` → `invoking` → `[knowledge][recovery] command_received` → `resume_requested` → `resume_decision=allowed` → `local_resume_started` → `lexical_progress`/`embedding_progress`. If any link is missing, its absence now names the exact break. Windows Knowledge remains NOT YET HUMAN-VALIDATED; M11 NOT STARTED; the older `inference_failed` runtime issue stays out of scope until the trigger is proven.
