## KNOWLEDGE K5A.1 DURABLE MATERIAL INDEXING STATUS COMPLETE (2026-09-05)

- **K5A.1 durable material indexing status — COMPLETE (`15cea99`):** schema v3 adds the project-local, Material-scoped durable `material_index_state` contract in `projects/<project-id>/knowledge/knowledge.sqlite`. `MaterialIndexState` is `Pending`, `Ready`, `Failed`, or `Unsupported`; accepted/stored project Material state remains deliberately separate from Knowledge success. `KnowledgeStore::material_index_status` is the typed query API, and `begin_material_indexing` / `index` / `retry_material_index` are the explicit synchronous indexing/retry boundary for later production wiring.
- **Semantics:** supported attempts commit `PENDING` before local derivation, then `READY` on successful K1 ingestion or typed/sanitized retryable `FAILED`; unsupported formats are durably `UNSUPPORTED`, never fake-ready. A crash-like PENDING remains visible and retryable after reopen, with no automatic background worker or silent READY normalization. `remove` transactionally cleans the Material source association and status record; same-byte Materials retain independent status while preserving K1 canonical reuse. Status records contain only IDs/timestamps/state/failure code—no source body, chunk text, query, arbitrary diagnostic, secret, or absolute path.
- **Migration/validation:** deterministic v2→v3 migration preserves documents, chunks/FTS, embedding generations/vectors, and retrieval. Focused tests cover success/reopen, invalid UTF-8 failure/retry, unsupported format, stale pending, delete, same-content aliases, project isolation, privacy shape, and K1/K2 migration preservation. MATERIAL INDEX STATUS MANAGEMENT REMOTE LLM CALLS: ZERO. No dependency, UI, background-worker, semantic-provider, Windows released-feature, or Linux packaging change.
- **Remaining K5 production work:** production material-index invocation wiring; production local semantic-provider wiring; mandatory K5 request-boundary acceptance matrix closure. **Next gate:** RESUME K5 PRODUCTION MATERIAL INDEXING + LOCAL SEMANTIC PROVIDER WIRING + ACCEPTANCE CLOSURE. Summarization is NOT STARTED. M11: NOT STARTED.
