## FEDORA KNOWLEDGE PRODUCTION OBSERVABILITY + MASS-ATTACHMENT E2E — IMPLEMENTED (2026-09-06)

- **Scope closed (bounded, no architecture redesign):** (B/C/D/E) Knowledge corpus/index + semantic-provider + retrieval/context observability; (F) typed agent terminal-failure classification; (G) provider-independent whole-project K6 summary routing; (J) mass-attachment composer layout + truthful duplicate copy. The previous Codex pass was stopped by the session-budget harness (`SESSION_BUDGET: UNKNOWN → exit 4`), an independent *harness* blocker, never a product blocker; this pass ran directly under OpenCode Go DeepSeek V4 Pro and did not touch `scripts/agent-launch`.
- **Observability (B/C/D/E):** `KnowledgeStore::corpus_stats()` reports sanitized `material_count / pending / ready / failed / unsupported / chunks_total / embeddings_ready / corpus_bytes / corpus_utf8_chars / naive_corpus_est_tokens` (no bodies/paths/vectors). `hybrid_search_metrics` returns `HybridSearchMetrics { lexical_candidates, semantic_candidates, fused_candidates, semantic_availability }`. `SemanticProviderState` replaces ambiguous unavailability with ten structural codes (`available` … `semantic_query_failed`, `unavailable_other`); `OrtEmbeddingProvider::load` now classifies failure into `OrtProviderLoadError::{RuntimeInitFailed, SessionBuildFailed, InputContractMismatch}` (no paths/error bodies leak). `prepare_knowledge_context` logs one sanitized `[knowledge] retrieval …` line per Knowledge turn: mode/evidence/budget/semantic+cause/candidate counts/evidence bytes+chars+est-tokens/naive corpus/`context_reduction_pct_vs_naive_corpus`/material-status counts/corpus totals. Indexing logs `embeddings_created/reused`. No remote embedding fallback introduced; lexical fallback preserved.
- **Return/turn metrics (D/E):** the retrieval log separates `lexical_candidates`, `semantic_candidates`, `fused_candidates`, `selected_evidence_count`, `evidence_bytes/utf8_chars/est_tokens`, `context_budget_limit/used`, and reports `naive_corpus_est_tokens` + `context_reduction_pct_vs_naive_corpus` (an *estimate vs naive full corpus*, named as such, not claimed exact savings). Remote LLM accounting: ingestion/embedding/query-embedding/retrieval/context-assembly remain ZERO (local E5); the only remote call is the final chat answer; summary remote calls are counted in `SummaryAccounting`. OpenCode exposes no trustworthy per-call input/output token counts on the `serve` surface used, so no fabricated "actual tokens" are logged — only labelled estimates (recorded here as a limitation, per policy).
- **120s failure classification (F):** `AgentError::task_failure_kind()` maps the bounded task outcome to `timeout | agent_execution | provider_transport | cancelled | unknown`. `OpenCodeAgentEngine` no longer collapses the 120s poll deadline (`DEFAULT_TASK = 120s` in `project-agent/src/opencode.rs`) into an opaque `TaskFailed("timed out")`; it now returns `Timeout`, so the terminal log emits `failure_stage=agent failure_class=timeout duration_ms=…`. OpenCode does not surface provider rate-limit/context/auth/internal causes structurable here, so those collapse to `agent_execution` — recorded explicitly rather than invented.
- **Whole-corpus K6 routing (G):** `detect_summarize_intent` (provider- and language-independent verb+scope detection, NOT a hardcoded `"resumime todos los archivos"` string; ordinary informational questions keep K3/K4 chat). Production `AppState` now stores the shared OpenCode backend and exposes `summarize_project_with_backend` → `OpenCodeRemoteSummarizer` (dedicated scratch session, no chat pollution). `agent_send` routes summary-intent turns through `send_summary_run`, which runs K6 (document→batch→global, bounded per-node labelled evidence) and appends the user-facing global summary as the assistant message. Never sends the corpus.
- **Mass-attachment UI (J):** `ComposerBar` collapses attachments above 8 into a bounded scrollable area with a truthful `"N archivos seleccionados · Ver todos"` toggle (off by default); the composer/Send button stay visible. Duplicate copy is now truthful: import distinguishes pre-existing (`duplicate` → "ya estaba en el proyecto") from same-batch (`duplicate_in_batch` → "ya estaba en esta selección"), so a new conversation showing "1 ya estaba" can no longer misreport a same-batch content duplicate as "already in the project". (The "51 agregados · 1 ya estaba" = 52 items for a 51-file drop is most consistent with the OS/drop delivering 52 paths — e.g. a hidden entry or a duplicated path — rather than a product counter bug; the truthful-copy fix removes the misleading "ya estaba en el proyecto" conflation regardless.)
- **Representative 51-file test (K):** `representative_51_markdown_corpus_flow` ingests 51 synthetic Markdown meeting notes through `import_materials`, asserts `ready=51`/`failed=0`/`unsupported=0`, measurable corpus + chunks, a bounded K3/K4 evidence package ≪ corpus, then K6 global (51 doc + bounded batches/global, depth ≥3, no synthesis request exceeding branching factor), with a full cache-hit second run (`remote_calls=0`). Non-sensitive fixtures only.
- **Validation:** `cargo fmt --all -- --check` PASS; `cargo clippy -p project-knowledge/-project-app/-project-agent --all-targets -- -D warnings` PASS; `cargo test -p project-knowledge` (35), `-p project-app` (incl. 12 summarization + 21 materials), `-p project-agent` (29), `cargo test --workspace` PASS; frontend `pnpm typecheck`/`pnpm lint`/`pnpm format:check`/`pnpm test` (245)/`pnpm build` PASS; `git diff --check` PASS. `./scripts/verify` reaches the workspace strict-Clippy gate and stops on the SAME two pre-existing `unused_mut` warnings in **unchanged** `project-tunnel/src/cloudflare.rs:291` and `project-opencode/src/lib.rs:154` (lib-test), reported unchanged, not broadened.
- **Remaining gates (not attempted here):** full Fedora AppImage rebuild + real E5/ONNX runtime re-validation on this host, real-model (billed) 51-document summarization, Windows Knowledge runtime human validation. **M11: NOT STARTED.** **Local E5 semantic availability** was not re-litigated in this pass because no AppImage/model-cache refresh changed; the K5 HUMAN-PASS record stands, and the new `SemanticProviderState` code paths are exercised structurally. Re-running the real AppImage workflow remains the next production gate.
