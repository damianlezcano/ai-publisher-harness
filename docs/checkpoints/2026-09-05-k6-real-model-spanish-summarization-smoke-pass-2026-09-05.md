## K6 REAL-MODEL SPANISH SUMMARIZATION SMOKE — PASS (2026-09-05)

- **Gate closed:** `K6 REAL-MODEL SPANISH SUMMARIZATION SMOKE: PASS`. The committed K6 hierarchical summarization path was exercised end-to-end against ONE real remote model with a tiny controlled Spanish corpus. This was a bounded runtime validation, not a quality benchmark, provider comparison, or implementation pass. **No production code changed.**
- **Provider/model:** the currently configured DeepSeek V4 Flash model (`opencode-go/deepseek-v4-flash`) through the real OpenCode 1.18.29 `serve` backend via the committed `OpenCodeRemoteSummarizer` dedicated scratch-session path (`crates/project-app/src/summarize.rs`). The scratch session ran in an isolated OpenCode config seeded only with the model selector, the user's existing `opencode-go` credential, and the provider catalog; the prompts were the real `build_document_summary_request` / `build_synthesis_request` instructions (Spanish, bounded labelled evidence only).
- **Corpus:** 3 controlled Spanish meeting-style documents (2 TXT, 1 Markdown): `reunion-infraestructura.txt` (OpenShift migration decided, deployment automation pending, INC-12345 storage issue, Carla must prepare the app inventory), `reunion-seguridad.md` (access-policy review requested, least privilege agreed, MFA undecided, Martín to prepare a proposal), `reunion-seguimiento.txt` (OpenShift still priority, inventory completed, deployment automation still pending, INC-12345 resolved).
- **Ingestion:** all 3 Materials went through the production `AppState::add_material_from_path` → `add_material_and_index` path; `material_index_state = Ready` for all 3, schema v4, 3 ready documents (1, 2, 1 chunks), lexical data available. DOCUMENT INGESTION / EMBEDDING / QUERY EMBEDDING / RETRIEVAL / CONTEXT ASSEMBLY REMOTE LLM CALLS: ZERO.
- **Hierarchy generated (deterministic default planner, depth 3):** 3 document summaries + 1 batch (grouping the 3 document summaries) + 1 global (synthesizing the batch). **FIRST K6 SUMMARIZATION REMOTE LLM CALLS: 5** (3 document + 1 intermediate/batch + 1 global). No artificial hierarchy depth was added.
- **Real structured validation:** the real model returned output accepted by the committed `validate_summary_output` for every node; all 5 nodes persisted `Ready`. The global Spanish summary correctly captured: OpenShift migration as priority/recurring topic; deployment automation pending; INC-12345 existed and was later resolved; security requested access-policy review / least privilege agreed; MFA with no final decision. **Cross-document temporal synthesis verified:** Carla's inventory, pending in meeting 1, is reported as completed in the later meeting (never claimed pending). Evidence labels resolved to the request set (E1.. at document level, P1 at batch/global level); the validator strips references outside the immediate request set by design, so downstream lineage labels appear as `[]` (lineage is preserved structurally through `source_ids`/`summary_sources`, never through forged references).
- **Provenance:** global → batch → 3 document summaries → source chunk ids; full lineage resolvable. **Cache proof (mandatory gate):** re-running EXACTLY the same `summarize_project` request (unchanged corpus and contract) returned **SUMMARY CACHE HIT**, `reused=5`, `cache_hits=5`, **SECOND IDENTICAL K6 SUMMARIZATION REMOTE LLM CALLS: ZERO**. No re-embedding, no re-ingestion, no hidden remote call.
- **Chat pollution:** internal scratch summarization sessions created no normal conversation turn (agent-engine send calls = 0; chat history unchanged). **Privacy:** session logs carried only structural metadata (ids, names, byte counts, version, durations); no document/chunk bodies, API keys, provider request/response, embeddings/vectors, DB internals, or absolute private app-data source paths.
- **Failure policy exercised naturally:** during one of the bounded re-runs a single document summary returned output rejected by the committed validator; K6 marked that node `Failed(InvalidOutput)` and did NOT synthesize a fake batch/global (downstream nodes correctly not attempted). No product defect — retry-after-failure recovers by design. The recorded PASS run was fully `Ready`.
- **Not claimed:** no 100-document real-provider validation, no summarization quality benchmark, no provider/model comparison, no Windows validation, no NotebookLM UX completion. M11: NOT STARTED. Windows Knowledge semantic runtime: NOT YET HUMAN-VALIDATED. No new blockers.
- **Bounded spend transparency:** total real remote LLM calls across the whole smoke: 1 trivial config/credential probe + 18 summarization calls across four tiny runs (one natural failed-node run of 3 calls correctly produced no fake global; three runs completed all 5 nodes). DeepSeek V4 Flash is nondeterministic, so a clean PASS run may occasionally need a bounded retry; each retry uses the tiny corpus only.
- **Validation (no production code changed):** `cargo fmt --all -- --check` PASS; `cargo test -p project-knowledge` PASS (34); `cargo test -p project-app` PASS (144 incl. 11 structural summarization tests); `git diff --check` PASS. The smoke used a temporary controlled harness (removed after the run; no files remain in the tree).
