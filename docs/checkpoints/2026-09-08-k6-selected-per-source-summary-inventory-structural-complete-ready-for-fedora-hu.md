## K6 SELECTED-PER-SOURCE SUMMARY INVENTORY — STRUCTURAL COMPLETE, READY FOR FEDORA HUMAN VALIDATION (2026-09-08)

- **Starting tree:** `35f910b` with uncommitted Codex work on typed `SummaryIntent`, `selected_material_ids`, and `summarize_selected_sources`. Continuation did not reset that tree. Independent OpenCode Qwen review via `scripts/agent-launch` could not start (`check-session-budget` exit 4 UNKNOWN on this Cursor session); a fresh independent read-only reviewer then returned **REVIEW=APPROVE**.
- **Root cause (verified):** explicit “resumí cada archivo” wording previously selected only a boolean K6 route over **every ready project document**. The accepted turn’s exact composer-selected material identities were not retained as the source inventory, so a five-file request could omit selected files or include historical materials. K3/K4 top-k is not an inventory.
- **Fix:** `detect_summary_intent` distinguishes `None` / `Project` / `SelectedPerSource`. Composer ids are stored on `AgentRunInputs.selected_material_ids` (not raw-forwarded). `send_summary_run_with` plans K6 over those identities only, keeps document/batch/global cache/accounting internally, and surfaces one labelled entry per selected source. Filename dates are trusted only as unambiguous `YYYY-MM-DD` / `YYYY_MM_DD`; ties and undated names keep composer order. Unusable selected sources remain as “No se pudo procesar este archivo.” Whole-project wording with **zero** attachments stays `Project`.
- **Tests:** exact five selected + historical exclusion; send-path identities + truthful `remote_calls=7` and `additional_attachment_route=false`; top-k ≠ inventory; date/noise/invalid calendar; explicit PDF failure; semantic chat stays non-K6; one-file selected path; durable TurnMetrics suite unchanged.
- **Validation:** `cargo fmt --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS; `cargo test --workspace` PASS; frontend format/typecheck/lint/test (282) / build PASS; `git diff --check` PASS; `scripts/test-distribution-contracts` PASS; `./scripts/verify` PASS.
- **Fresh AppImage (from HEAD `d08099e`, Ubuntu 24.04 Podman; documented linuxdeploy → appimagetool fallback):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,132,152 bytes**, SHA-256 `6fb3170c647b64b2e41ba1065a9dcf777316b87f51ace8bee720b22600bd158e`. GLIBC max **2.39** (138 ELF). WebKitGTK relocatable runtime PASS. Graphics gate PASS. ONNX Runtime **1.22.0** payload PASS. Bundled OpenCode **1.18.25**. Bundled cloudflared **2026.8.3**. `scripts/test-distribution-contracts` PASS. Artifact not committed.
- **Gate status:** structural/regression complete. **Do not claim Fedora GUI PASS.** Windows Knowledge remains NOT YET HUMAN-VALIDATED. M11 NOT STARTED. Not pushed.
