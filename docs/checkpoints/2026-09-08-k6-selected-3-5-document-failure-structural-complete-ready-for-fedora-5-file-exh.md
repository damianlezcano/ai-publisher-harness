## K6 SELECTED 3/5 DOCUMENT FAILURE — STRUCTURAL COMPLETE, READY FOR FEDORA 5-FILE EXHAUSTIVE-SUMMARY HUMAN VALIDATION (2026-09-08)

- **Starting tree:** `144e6aa` clean except untracked local `opencode.json` (not committed). Author: Cursor Agent. Independent read-only reviewer: **REVIEW=APPROVE**.
- **Human Fedora evidence (authoritative, post-poll AppImage):** five selected Markdown files indexed (`embeddings_created=4655`), date order correct, ~34s (not ~600s). Terminal: `documents=5 summary_nodes_generated=3 remote_summary_calls=5`. UI: 17-Jul and 27-Jul `"No se pudo procesar este archivo."`, three later files real summaries. Restart: conversation + durable metrics survived; details caused no new work; a second `"me haces un resumen de todos los archivos"` showed `reused=3 generated=0 remote_calls=2` and remained incomplete. Indexing/selected-set/date-order/TurnMetrics are not the blocker.
- **Exact mismatch:** five document `summarize()` calls returned text (`remote_calls=5`) but only three nodes stored `Ready`. Batch/global correctly did not run (`EmptyCorpus` when a child is not Ready). Accounting therefore cannot be 7 until all five document nodes succeed. Restart retried the two Failed nodes; they still did not persist Ready. That is `InvalidOutput` (and/or a stale `finish:"stop"` on a reused scratch session), not the old 120s poll miss.
- **Fix:** bind poll completion to messages that appeared after this prompt (ignore stale stops; accept `json` parts; abort the scratch session after success); unique FakeServer sessions; actually bound document evidence; accept fenced/partial JSON and nonempty prose; one `invalid_output` retry. Failed document nodes stay retryable. Batch/global still do not invent a global from an incomplete child set; the selected-source UI keeps successful siblings and an explicit per-source failure.
- **Tests:** stale-stop unit + reused-session FakeServer; production-faithful `five_staged_ready_markdown_files_generate_k6_nodes_through_opencode_envelope` (5 `[E1]` + 2 `[P1]` prompts, `remote_calls` equals executed prompts, unique sessions, no failure copy, date order, historical excluded, durable metrics, zero K6 on reopen); `one_genuine_selected_source_failure_stays_explicit_and_skips_batch_global`.
- **Validation:** `cargo fmt --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS; `cargo test --workspace` PASS; frontend format/typecheck/lint/test (282) / build PASS; `git diff --check` PASS; `scripts/test-distribution-contracts` PASS; `./scripts/verify` PASS (`CI=true`).
- **Fresh AppImage (from HEAD `e627bc6`, Ubuntu 24.04 Podman; documented linuxdeploy → appimagetool fallback):** `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **159,144,440 bytes**, SHA-256 `74031119f52557c6fe6604c7ac5547f5aaf446b17cef3366097bc57ca4a03c6b`. GLIBC max **2.39** (138 ELF). WebKitGTK relocatable runtime PASS. Graphics gate PASS. ONNX Runtime **1.22.0** payload PASS. Bundled OpenCode **1.18.25**. Bundled cloudflared **2026.8.3**. `scripts/test-distribution-contracts` PASS. Artifact not committed.
- **Gate status:** structural/regression complete. **Do not claim Fedora GUI PASS.** Windows Knowledge remains NOT YET HUMAN-VALIDATED. M11 NOT STARTED. Not pushed.
