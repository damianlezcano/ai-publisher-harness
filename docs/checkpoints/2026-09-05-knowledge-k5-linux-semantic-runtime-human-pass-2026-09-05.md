## KNOWLEDGE K5 LINUX SEMANTIC RUNTIME — HUMAN-PASS (2026-09-05)

- **Gate closed:** `KNOWLEDGE K5 LINUX SEMANTIC RUNTIME: HUMAN-PASS`. The REAL production Knowledge semantic flow was exercised end-to-end on Fedora (this host) with the REAL local `intfloat/multilingual-e5-small` fp32 model and the REAL bundled Microsoft ONNX Runtime CPU 1.22.0, through the real application/runtime stack. Windows is deliberately untouched and still reads: **CURRENT WINDOWS RELEASED FEATURE SET: HUMAN-PASS** (unchanged meaning) vs **KNOWLEDGE WINDOWS SEMANTIC RUNTIME: NOT YET HUMAN-VALIDATED** (separate later gate).
- **Runtime (Gate A):** `runtime_library_from_executable(current_exe)` resolved the bundled runtime with no cwd, PATH, or ORT environment dependence; the binary's `DT_NEEDED` contains no `onnxruntime` (dlopen-only), and no host `libonnxruntime` exists in the ldconfig cache (host ORT not required). Model generation/revision verified through `ModelManager::inspect == Verified` against the committed `config/knowledge-models.json` (all six artifact checksums, incl. `model.onnx` `ca456c06…` 470,268,510 bytes and `tokenizer.json` `0b44a9d7…`), tokenizer loads, `OrtEmbeddingProvider::load` succeeded (≈12.3 s cold init, 470 MiB fp32, single thread).
- **Production ingestion (Gates B/C):** real `AppState::add_material_from_path` accepted a Spanish TXT and a Spanish Markdown Material through the production boundary; both reached durable `Ready`; K1 lexical FTS succeeded; the production ingestion path itself generated local E5 embeddings (subsequent `index_embeddings` returned `embedded=0, reused=4/5`); Markdown headings survive as provenance (`["Automatizacion de clusters OpenShift"]`, `structural_type=heading`); accepted Materials remained present.
- **Real semantic query (Gate D):** `¿Como puedo automatizar operaciones sobre varios clusters de OpenShift?` → local query embedding executed, `semantic_availability=Available`, K3 hybrid returned the OpenShift-automation chunk first (`semantic_rank=1`) ahead of the unrelated photosynthesis chunk; query vector dims=384, L2 norm 1.000000, provider local (ONNX CPU + E5). **Exact identifier (Gate E):** `INC-12345` ranked first with `exact_identifier_match=true`, lexical and semantic rank both 1; hybrid did not degrade exact identifier preference.
- **Lexical fallback (Gate F):** with the provider absent (no model cache) K3 returned lexical-only results (`semantic_availability=Unavailable`) and the full production `prepare_knowledge_context` path still injected bounded evidence and completed the chat turn; no remote embedding fallback occurred; Material state/content intact. Runtime/model state restored afterwards.
- **K4 bounded context (Gate G):** `assemble_context` produced 2 evidence entries, `estimated_budget_used=167 <= 2700`, ≤8 entries, per-document/source caps, provenance retained, excerpts UTF-8, unrelated corpus excluded, total excerpt chars 355 ≪ corpus 538; no duplicate corpus expansion.
- **K5 request boundary (Gate H, capture backend):** the single outgoing request contained the bounded `<knowledge_evidence trust="untrusted">` block with `E1…` labels and the `INC-12345` evidence, and did NOT contain the app-data absolute path, `knowledge.sqlite`, `materials/N-…` raw indexed attachments (dedup), `onnx/model.onnx`, `knowledge-models`, embedding/vector data, or unrelated corpus text. One fake send per chat turn.
- **Calls (Gate I):** DOCUMENT INGESTION / EMBEDDING / QUERY EMBEDDING / RETRIEVAL / CONTEXT ASSEMBLY REMOTE LLM CALLS: ZERO. FINAL CHAT REMOTE LLM CALLS DURING VALIDATION: ZERO (fake/capture backend). **Privacy (Gate J):** session logs carried only structural/sanitized metadata; document body, chunk text, query content, secrets and the absolute app-data path never appeared in logs.
- **Restart/reuse (Gate K):** reopen persisted the Knowledge SQLite, model cache reused (still `Verified`, no re-download), unchanged documents were not re-embedded (`embedded=0, reused=4`), and semantic retrieval still worked after reopen. **Performance sanity (Gate L, cold CPU):** provider init ≈12.3 s; first document embedding (4 chunks, 1 batch) ≈49 ms; production index+embed first path ≈68 ms; repeated unchanged path 0 ms; query embedding + hybrid retrieval ≈11 ms; K4 assembly ≈0 ms. These are sanity measurements only, not product SLAs.
- **Packaged AppImage evidence (controlled rebuild from HEAD `5a28c81`):** `./scripts/package linux-appimage` (Ubuntu 24.04 podman) produced `app/src-tauri/target/release/bundle/appimage/EducAI_0.1.0_amd64.AppImage`, **158,898,680 bytes**, **SHA-256 `c1dca1f4972f4c799f30302679aea25dcf4d0d0838737918dff0d89d4f280775`**, timestamp 2026-09-05T18:47-03. All gates ran inside the build: `check-appimage-glibc` PASS (138 ELF, all ≤ GLIBC 2.39), `check-appimage-webkit-runtime` PASS, `check-graphics-appdir` PASS, `check-appimage-onnxruntime` PASS (ONNX Runtime under `usr/lib/educai/onnxruntime/`, GLIBC core 2.27 / provider 2.2, model/tokenizer excluded from the AppImage). The runtime-path gate harness was also executed from inside the extracted AppDir (`usr/bin/educai-runtime-gate`): `current_exe` → `runtime_library_from_executable` → `usr/lib/educai/onnxruntime/libonnxruntime.so` — real packaged-path E5 inference, ALL GATES PASS.
- **Genuine bounded fix (stale app lockfile):** the packaged build surfaced that `app/src-tauri/Cargo.lock` had not been updated when K5 wired `project-app` → `project-knowledge`; the container build regenerated it with the K5 dependency closure (`project-knowledge`, `ort`, `tokenizers`, `ndarray`, `rusqlite`, `reqwest`+rustls, etc.). Committed so packaged builds are reproducible.
- **Validation:** `cargo fmt --all -- --check` PASS; `cargo clippy -p project-knowledge --all-targets -- -D warnings` PASS; `cargo clippy -p project-app --all-targets -- -D warnings` PASS; `cargo test -p project-knowledge` PASS (26); `cargo test -p project-app` PASS (132); `cargo test --workspace` PASS; `git diff --check` PASS; `./scripts/test-distribution-contracts` PASS. `./scripts/verify` reached the workspace strict-Clippy gate and stopped on the two documented pre-existing `unused_mut` warnings in unchanged `project-tunnel` / `project-opencode` lib-test code (out of scope for this pass). The verified model cache now lives at the production app-data root `knowledge-models/intfloat--multilingual-e5-small/ccc66d3bcd826f577e26b9a4072cc5fe3a7ad6a3/` (outside the AppImage). No Windows work was performed in this pass. Summarization is NOT STARTED. M11: NOT STARTED. No new blockers.
