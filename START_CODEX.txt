New EducAI engineering session.

1. Read `CODEX_HANDOFF.md`, then `AGENTS.md`.
2. Follow the authority order those files name (product → harness docs under
   `docs/` → selected requirement under `tasks/` → `RUNTIME.md` only when
   routing a role).
3. Do not treat this file as policy, architecture, or durable state.

When a requirement ID is named (for example `Implementar REQ-XXXX.`), that is
a complete Orchestrator assignment: execute `prompts/orchestrator.md`.
