{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: event listen + file open dialog only.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "dialog:allow-open"
  ]
}
