# Baseline summary

Baseline: `015d466` (`main == origin/main` when inspected).

Evidence in Git, `docs/CURRENT_CHECKPOINT.md`, `docs/architecture/ARCHITECTURE.md`, and
named suites establishes these completed blocks: turn routing; OrdinaryChat /
Knowledge separation; semantic authority and exclusive fallback; bounded
continuity and session roles; material scope; Creation session boundary;
scratch completion/permission safety; grounded Knowledge answers; thematic and
exhaustive corpus routes; local inventory; compact PerItem; K6 durability;
large-import recovery; assistant/turn metrics; immutable Creation versions;
publication/version behavior; Linux AppImage and Windows runtime/distribution
gates; and the recorded human regression checkpoints.

Canonical tests include `project-app` `knowledge`, `inventory`, `large_import`,
`exhaustive_rag`, `app_facade`; `project-agent` `agent_service` and
`agent_security`; project filesystem/publication/publisher suites; frontend
tests; and `CI=true ./scripts/verify`. Key supporting commits include
`015d466`, `6a5d58c`, `4e215d2`, `e627bc6`, `09c588b`, `aeaddfa`, `1eeae03`,
and `0080e86`.

No dates, reviewers, or task IDs are inferred beyond this evidence.
